import { MonthlyGrowthChart } from "./MonthlyGrowthChart"

export const AnalyticsView = () => {
  // Analytics, e.g. month-on-month growth, financial category breakdown for biggest leaf categories, etc
  return <MonthlyGrowthChart />
}
